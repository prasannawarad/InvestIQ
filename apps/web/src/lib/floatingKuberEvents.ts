/** Floating Kuber panel visibility — KuberOrb and other canvases pause when open. */
export const FLOATING_KUBER_VISIBILITY = "investiq:floating-kuber-visibility";

export type FloatingKuberVisibilityDetail = { open: boolean };
